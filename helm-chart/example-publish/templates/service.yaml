apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-publish.fullname" . }}
  labels:
    {{- include "example-publish.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-publish.selectorLabels" . | nindent 4 }}
